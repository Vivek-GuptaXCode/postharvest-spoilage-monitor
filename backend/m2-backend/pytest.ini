[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    unit: Unit tests (no emulator needed)
    integration: Integration tests (needs Firestore emulator)
    e2e: End-to-end tests (needs all emulators)